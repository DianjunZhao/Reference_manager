# LatticeLens 1.0.7 (107) — current manual acceptance

Status: **NOT_RUN**. This fail-closed worksheet applies only to the exact
`LatticeLens-1.0.7-local.dmg` candidate and cannot reuse an observation for an
older installed executable.

Use only `/Applications/LatticeLens.app` whose Finder information says
**Version 1.0.7 (107)**. Do not inspect, migrate, export, delete, or otherwise
open a real Application Support library, research-PDF library, or Keychain.

For the changed Evidence behavior, choose a disposable or sanitized paper with
an arXiv ID, explicitly read its ar5iv HTML, then generate one important
formula derivation. A valid formula whose displayed source anchor is an excerpt
of that just-read HTML must complete; it must not fail with `v2 全文anchor
不属于本次受限document payload`. The card must remain anchored to the same
paper and extracted excerpt. A PDF source remains page-addressable and must
still reject a mismatched or missing page. A different paper, document hash,
anchor ID, quote, quote hash, section, source scope, or unanchored numerical
claim must remain a visible failure.

A machine-readable receipt may bind this observation to `manifest-v1.0.7.json`
only after the exact installed bundle and DMG hashes are verified. This
worksheet does not establish a real provider's availability, model quality,
INSPIRE availability, Keychain behavior, or notarization.
