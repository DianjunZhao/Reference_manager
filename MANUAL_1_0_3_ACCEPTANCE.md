# LatticeLens 1.0.3 (103) — current manual acceptance

Status: **NOT_RUN**.  This is a fail-closed worksheet for the exact
`LatticeLens-1.0.3-local.dmg` candidate.  It is not a receipt and it does not
reuse a receipt for an older executable.

Use only `/Applications/LatticeLens.app` whose Finder information says
**Version 1.0.3 (103)**.  Do not inspect, migrate, export, delete, or otherwise
open a real Application Support library, research-PDF library, or Keychain.

For the changed Evidence behavior, read a disposable/sanitized ar5iv or PDF
fixture, generate one formula derivation, and record one of the following exact
observations:

- PASS: a complete, anchor-bound formula derivation is shown; or
- TLS BLOCKED: the failure names only the endpoint host and port, says that no
  full-text evidence was automatically retried, and directs the user to
  Settings **测试连接** while retaining HTTPS certificate validation.

The Settings probe is an explicit user action that sends no paper full text.
It must not be used to bypass a TLS failure by changing to HTTP or accepting an
untrusted certificate.  A machine-readable receipt may bind this observation
to `manifest-v1.0.3.json` only after the exact installed bundle and its DMG
hashes have been verified.
