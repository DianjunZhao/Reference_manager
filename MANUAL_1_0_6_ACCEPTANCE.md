# LatticeLens 1.0.6 (106) — current manual acceptance

Status: **NOT_RUN**. This fail-closed worksheet applies only to the exact
`LatticeLens-1.0.6-local.dmg` candidate and cannot reuse an observation for an
older installed executable.

Use only `/Applications/LatticeLens.app` whose Finder information says
**Version 1.0.6 (106)**. Do not inspect, migrate, export, delete, or otherwise
open a real Application Support library, research-PDF library, or Keychain.

For the changed Evidence behavior, request one important-formula derivation
from a disposable/sanitized source with existing full-text anchors. A response
with a bare TeX command such as `\\alpha` must not fail solely with
`string escape无效`. If the provider instead emits
`physics.research_question` as a scalar string, that unanchored text must not
be accepted as a direct physics statement: the app must mark the research
question as unavailable and may display the remaining formula derivations only
when they independently satisfy the existing source-scope, anchor, and numeric
provenance checks. A scalar question must not by itself prevent an otherwise
valid anchored formula card from completing.

Missing or invalid required fields other than that one compatibility shape,
`null`, a whitespace-only abstract, malformed `\\u` unicode escape,
non-allowlisted anchors, source-scope errors, and unanchored numerical claims
must remain visible failures. A machine-readable receipt may bind this
observation to `manifest-v1.0.6.json` only after the exact installed bundle and
DMG hashes are verified.
