# LatticeLens 1.0.5 (105) — current manual acceptance

Status: **NOT_RUN**. This fail-closed worksheet applies only to the exact
`LatticeLens-1.0.5-local.dmg` candidate and cannot reuse an observation for an
older installed executable.

Use only `/Applications/LatticeLens.app` whose Finder information says
**Version 1.0.5 (105)**. Do not inspect, migrate, export, delete, or otherwise
open a real Application Support library, research-PDF library, or Keychain.

For the changed Evidence behavior, request one important-formula derivation
from a disposable/sanitized source with existing full-text anchors. A response
containing a TeX command such as `\\alpha` must not fail solely with
`string escape无效`: the app preserves it as a literal TeX command before
strict schema validation. Missing or invalid required fields, `null`, a
whitespace-only abstract, malformed `\\u` unicode escape, non-allowlisted
anchors, source-scope errors and unanchored numerical claims must remain visible
failures.

The Settings probe is an explicit user action that sends no paper full text. It
must not be used to bypass a TLS failure by switching to HTTP or accepting an
untrusted certificate. A machine-readable receipt may bind this observation to
`manifest-v1.0.5.json` only after the exact installed bundle and DMG hashes are
verified.
