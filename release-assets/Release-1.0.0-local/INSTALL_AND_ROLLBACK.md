# LatticeLens 1.0.0 — same-Mac local artifact

This DMG is ad-hoc signed for local inspection. It is not Developer ID signed or notarized.

Install by verifying SHA256SUMS.txt, then drag LatticeLens.app to /Applications. This rebuild contains the native Unicode TeX renderer. Uninstall removes only the app bundle; keep the library and PDFs.
