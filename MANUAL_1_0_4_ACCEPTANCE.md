# LatticeLens 1.0.4 (104) — current manual acceptance

Status: **NOT_RUN**. This fail-closed worksheet applies only to the exact
`LatticeLens-1.0.4-local.dmg` candidate and cannot reuse an observation for an
older installed executable.

Use only `/Applications/LatticeLens.app` whose Finder information says
**Version 1.0.4 (104)**. Do not inspect, migrate, export, delete, or otherwise
open a real Application Support library, research-PDF library, or Keychain.

For the changed Evidence behavior, use a disposable/sanitized source whose
metadata has no original abstract, then request one important-formula
derivation. Record that either a complete anchor-bound derivation appears, or
that a normal provider/network/schema failure is displayed. In particular, the
old `abstract_zh不是受限非空字符串` rejection must not be reported merely
because the supplied source abstract is absent. This does not permit missing,
`null`, whitespace-only, or invented abstracts; anchor, source-scope and
numeric-provenance failures must remain visible failures.

The Settings probe is an explicit user action that sends no paper full text. It
must not be used to bypass a TLS failure by switching to HTTP or accepting an
untrusted certificate. A machine-readable receipt may bind this observation to
`manifest-v1.0.4.json` only after the exact installed bundle and DMG hashes are
verified.
